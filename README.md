# NC

Nix Collective: host and module based Nix, Home Manager, and nix-darwin configuration.

This starts in the same broad style as RGBCube/ncc: flake-parts at the root, hosts under `hosts/`, reusable modules under `modules/`, and auto-imported `*.mod.nix` flake modules.
